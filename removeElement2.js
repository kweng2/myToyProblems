var removeElements = function(head, val) {
  // lets deal with edge cases right off the bat
  // If head is blank, return nothing
  
};

function ListNode(val) {
  this.val = val;
  this.next = null;
}

var a = new ListNode(1);
var b = new ListNode(2);
var c = new ListNode(6);
a.next = b;
b.next = c;
c.next = d;